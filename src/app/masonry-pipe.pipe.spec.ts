import { MasonryPipePipe } from './masonry-pipe.pipe';

describe('MasonryPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MasonryPipePipe();
    expect(pipe).toBeTruthy();
  });
});
